---
phase: 11-polish-and-testing
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - controllers/goldstandard.controller.ts
autonomous: true
requirements:
  - PORT-18

must_haves:
  truths:
    - "Gold standard API URL includes source=publication-manager parameter"
    - "The goldstandard.controller.ts fetch URL includes &source=publication-manager (enabling downstream audit attribution when the ReCiter Java API logs the call)"
  artifacts:
    - path: "controllers/goldstandard.controller.ts"
      provides: "Source-tagged gold standard API call"
      contains: "source=publication-manager"
  key_links:
    - from: "controllers/goldstandard.controller.ts"
      to: "ReCiter Java API"
      via: "fetch URL query parameter"
      pattern: "source=publication-manager"
---

<objective>
Append `&source=publication-manager` to the gold standard API URL in goldstandard.controller.ts, per D-17 and D-18. This one-line deferred gap from Phase 10 enables the ReCiter Java audit log to correctly attribute gold standard changes to Publication Manager vs other API clients.

Purpose: Audit attribution for gold standard changes -- enables operational visibility into which client made each change.
Output: Updated goldstandard.controller.ts with source parameter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-polish-and-testing/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append source=publication-manager to gold standard API URL</name>
  <files>controllers/goldstandard.controller.ts</files>
  <read_first>
    - controllers/goldstandard.controller.ts (current state -- line 13 is the fetch URL)
  </read_first>
  <action>
On line 13 of `controllers/goldstandard.controller.ts`, change the fetch URL from:

```typescript
return fetch(`${reciterConfig.reciter.reciterUpdateGoldStandardEndpoint}?goldStandardUpdateFlag=${goldStandardUpdateFlag}`, {
```

to:

```typescript
return fetch(`${reciterConfig.reciter.reciterUpdateGoldStandardEndpoint}?goldStandardUpdateFlag=${goldStandardUpdateFlag}&source=publication-manager`, {
```

This is a single-line change. The `&source=publication-manager` query parameter is appended after the existing `goldStandardUpdateFlag` parameter. No other changes to the file.
  </action>
  <verify>
    <automated>grep -q 'source=publication-manager' controllers/goldstandard.controller.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - controllers/goldstandard.controller.ts line 13 contains `&source=publication-manager` in the fetch URL template literal
    - The URL structure is: `${endpoint}?goldStandardUpdateFlag=${flag}&source=publication-manager`
    - No other lines in the file are modified
    - File still compiles (no TypeScript errors): `npx tsc --noEmit controllers/goldstandard.controller.ts 2>&1` or verified by `npx next build`
  </acceptance_criteria>
  <done>Gold standard API URL includes source=publication-manager parameter for audit attribution</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Publication Manager -> ReCiter Java API | Adding source parameter to outbound API call for audit logging |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | R (Repudiation) | goldstandard.controller.ts | mitigate | The `source=publication-manager` parameter specifically addresses repudiation by enabling ReCiter's audit log to attribute gold standard changes to Publication Manager. This is the mitigation itself. |
| T-11-06 | T (Tampering) | source parameter | accept | The source parameter is a static string, not user-controlled input. Server-side only. No injection risk. |
</threat_model>

<verification>
1. `grep 'source=publication-manager' controllers/goldstandard.controller.ts` matches line 13
2. No other changes in the file
</verification>

<success_criteria>
- goldstandard.controller.ts contains `&source=publication-manager` in the fetch URL
- Change is exactly one line modified
</success_criteria>

<output>
After completion, create `.planning/phases/11-polish-and-testing/11-04-SUMMARY.md`
</output>
