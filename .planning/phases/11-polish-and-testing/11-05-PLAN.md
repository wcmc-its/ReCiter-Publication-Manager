---
phase: 11-polish-and-testing
plan: 05
type: execute
wave: 2
depends_on:
  - 11-01
  - 11-02
  - 11-03
  - 11-04
files_modified: []
autonomous: false
requirements:
  - PORT-18
  - PORT-19
  - PORT-20
  - A11Y-01

must_haves:
  truths:
    - "Skeleton loading renders correctly in dev mode under React 18 StrictMode"
    - "No doubled skeleton elements from StrictMode double-render"
    - "Skeleton shimmer animations play smoothly"
    - "Full test suite passes (npm test)"
    - "Zero jsx-a11y violations"
  artifacts: []
  key_links:
    - from: "next.config.js"
      to: "CSS skeleton rendering"
      via: "reactStrictMode: true"
      pattern: "reactStrictMode"
---

<objective>
Final phase verification: visually confirm CSS-based skeleton loading works under React 18 StrictMode (per D-01, D-02, D-03), run the full test suite, and validate zero a11y violations. This plan gates the phase as complete.

Purpose: Ensures all four phase requirements (PORT-18, PORT-19, PORT-20, A11Y-01) are satisfied before the phase is marked complete.
Output: Verified phase -- all requirements met.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-polish-and-testing/11-CONTEXT.md
@.planning/phases/11-polish-and-testing/11-01-SUMMARY.md
@.planning/phases/11-polish-and-testing/11-02-SUMMARY.md
@.planning/phases/11-polish-and-testing/11-03-SUMMARY.md
@.planning/phases/11-polish-and-testing/11-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and a11y audit</name>
  <files>None (verification only)</files>
  <read_first>
    - jest.config.js (verify node config)
    - jest.config.dom.js (verify jsdom config)
    - .eslintrc.json (verify jsx-a11y strict mode)
    - next.config.js (verify reactStrictMode: true)
  </read_first>
  <action>
Run three verification commands:

1. Full test suite: `npm test`
   Expected: All tests pass (27 existing utility tests + 5 checkCurationScope tests via jest.config.js, then component tests via jest.config.dom.js)

2. A11y audit: `npx eslint --ext .tsx,.jsx src/ 2>&1 | grep "jsx-a11y"`
   Expected: Zero matches (no jsx-a11y violations)

3. Verify StrictMode is enabled: `grep -q "reactStrictMode.*true" next.config.js && echo "StrictMode enabled" || echo "StrictMode NOT enabled"`
   Expected: "StrictMode enabled"

4. Verify gold standard source tag: `grep -q "source=publication-manager" controllers/goldstandard.controller.ts && echo "Source tag present" || echo "Source tag MISSING"`
   Expected: "Source tag present"

5. Start dev server for skeleton visual verification:
   ```
   /Users/paulalbert/.claude/scripts/port-clear.sh 3000
   ```
   Then start the Next.js dev server in background on port 3000.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with all tests passing
    - `npx eslint --ext .tsx,.jsx src/ 2>&1 | grep -c "jsx-a11y"` outputs `0`
    - `grep "reactStrictMode" next.config.js` shows `true`
    - `grep "source=publication-manager" controllers/goldstandard.controller.ts` matches
  </acceptance_criteria>
  <done>Full test suite green, zero a11y violations, StrictMode confirmed enabled, source tag confirmed present</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of skeleton loading and a11y fixes</name>
  <files>None (visual verification only)</files>
  <action>
Present the following verification checklist to the user. Phase 11 implementation is complete:
- Jest 29 dual-config test infrastructure (jest.config.js for node, jest.config.dom.js for jsdom) with passing scope/proxy RTL tests
- 58 jsx-a11y violations fixed across 22 component files (labels in Plan 02, interactive elements in Plan 03)
- Gold standard audit source tag added (Plan 04)
- CSS-based skeleton loading ready for visual verification under React 18 StrictMode

Verification steps for the user:

1. Skeleton Loading (PORT-18): In the running dev server (http://localhost:3000):
   - Navigate to a curation page (e.g., /curate/[any-id]) to observe skeleton cards during loading
   - Navigate to Reports page to observe skeleton cards during data fetch
   - Verify: skeletons render exactly once (no doubling), shimmer animation is smooth, no flicker
   - If the page loads too fast to see skeletons, throttle network in browser DevTools (Slow 3G)

2. A11y Fixes (A11Y-01): Tab through interactive elements on:
   - AddUser page -- form labels should be associated with inputs
   - Header -- keyboard navigation should work on all action items
   - Report page -- export modal, checkboxes, date pickers should all be keyboard-accessible

3. Test Suite (PORT-19, PORT-20): Run `npm test` in terminal -- all tests should pass

4. Source Tag (PORT-18): Open controllers/goldstandard.controller.ts -- line 13 should contain `&source=publication-manager`
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>User confirms skeleton loading, a11y fixes, test suite, and source tag are all correct</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan is verification-only; no code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | I (Information Disclosure) | Dev server for verification | accept | Dev server runs locally only (localhost:3000). No production exposure. Killed after verification. |
</threat_model>

<verification>
1. npm test exits 0
2. eslint jsx-a11y violations = 0
3. Human verifies skeleton loading visually
4. Human verifies keyboard accessibility of fixed elements
5. Gold standard source tag confirmed via grep
</verification>

<success_criteria>
- All automated checks pass (tests, eslint, grep)
- Human confirms skeleton loading renders correctly under StrictMode
- Phase 11 can be marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/11-polish-and-testing/11-05-SUMMARY.md`
</output>
