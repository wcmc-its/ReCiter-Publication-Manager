---
phase: 05-phase3-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-scoped-curation-roles/03-VERIFICATION.md
autonomous: true
requirements: [SCOPE-01, SCOPE-02, SCOPE-03, SCOPE-04, SCOPE-05, SCOPE-06]

must_haves:
  truths:
    - "Every SCOPE requirement (01-06) has a status of SATISFIED, PARTIAL, or FAILED with specific evidence"
    - "All 36 observable truths from Phase 3 plans have VERIFIED, WARN, or FAILED status with file+line evidence"
    - "All 14 key links from Phase 3 plans have WIRED or BROKEN status with import/call chain evidence"
    - "Anti-patterns section lists any TODO/FIXME/stub/placeholder found in Phase 3 files"
    - "Human Verification Required section covers visual/interactive behaviors that cannot be confirmed from code"
    - "Test Coverage Assessment maps each SCOPE requirement to existing test coverage and notes gaps"
  artifacts:
    - path: ".planning/phases/03-scoped-curation-roles/03-VERIFICATION.md"
      provides: "Independent verification report for Phase 3 Scoped Curation Roles"
      contains: "Observable Truths"
  key_links:
    - from: ".planning/phases/03-scoped-curation-roles/03-VERIFICATION.md"
      to: "SCOPE-01 through SCOPE-06"
      via: "Requirements Coverage table"
      pattern: "SCOPE-0[1-6]"
---

<objective>
Independently verify all 6 SCOPE requirements against the actual codebase, producing a VERIFICATION.md in the Phase 3 directory.

Purpose: Phase 3 implemented scoped curation roles across 5 plans but was never independently verified. The milestone audit identified this as the primary gap. This verification reads actual source files (not SUMMARY claims), runs existing tests, traces data flow connections, and produces a structured VERIFICATION.md following the Phase 4 format.

Output: `.planning/phases/03-scoped-curation-roles/03-VERIFICATION.md` with Observable Truths, Required Artifacts, Key Link Verification, Requirements Coverage, Anti-Patterns, Human Verification Required, Test Coverage Assessment, and Gaps Summary.
</objective>

<execution_context>
@/Users/paulalbert/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paulalbert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-phase3-verification/05-CONTEXT.md
@.planning/phases/05-phase3-verification/05-RESEARCH.md
@.planning/phases/03-scoped-curation-roles/03-CONTEXT.md

<!-- Phase 3 PLAN frontmatter contains must_haves.truths (36 items) and must_haves.key_links (14 items) -->
@.planning/phases/03-scoped-curation-roles/03-01-PLAN.md
@.planning/phases/03-scoped-curation-roles/03-02-PLAN.md
@.planning/phases/03-scoped-curation-roles/03-03-PLAN.md
@.planning/phases/03-scoped-curation-roles/03-04-PLAN.md
@.planning/phases/03-scoped-curation-roles/03-05-PLAN.md

<!-- Phase 3 SUMMARYs provide claims to verify AGAINST (not to trust) -->
@.planning/phases/03-scoped-curation-roles/03-01-SUMMARY.md
@.planning/phases/03-scoped-curation-roles/03-02-SUMMARY.md
@.planning/phases/03-scoped-curation-roles/03-03-SUMMARY.md
@.planning/phases/03-scoped-curation-roles/03-04-SUMMARY.md
@.planning/phases/03-scoped-curation-roles/03-05-SUMMARY.md

<!-- Phase 4 VERIFICATION.md is the format template -->
@.planning/phases/04-curation-proxy/04-VERIFICATION.md

<!-- Phase 3 VALIDATION.md for test coverage assessment -->
@.planning/phases/03-scoped-curation-roles/03-VALIDATION.md

<interfaces>
<!-- This plan reads source files -- no interfaces to extract. The key source files are listed
     in the task read_first blocks. The output is a markdown document, not code. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify all 36 observable truths and 14 key links against source code</name>
  <files>.planning/phases/03-scoped-curation-roles/03-VERIFICATION.md</files>
  <read_first>
    <!-- FORMAT TEMPLATE -->
    .planning/phases/04-curation-proxy/04-VERIFICATION.md

    <!-- PHASE 3 PLAN FRONTMATTER (contains the 36 truths and 14 key links to verify) -->
    .planning/phases/03-scoped-curation-roles/03-01-PLAN.md
    .planning/phases/03-scoped-curation-roles/03-02-PLAN.md
    .planning/phases/03-scoped-curation-roles/03-03-PLAN.md
    .planning/phases/03-scoped-curation-roles/03-04-PLAN.md
    .planning/phases/03-scoped-curation-roles/03-05-PLAN.md

    <!-- 16 KEY SOURCE FILES TO VERIFY (read ALL of these) -->
    src/db/models/AdminUsersPersonType.ts
    src/utils/scopeResolver.ts
    src/utils/constants.js
    src/db/models/init-models.ts
    controllers/db/userroles.controller.ts
    src/pages/api/auth/[...nextauth].jsx
    src/middleware.ts
    controllers/db/person.controller.ts
    src/pages/api/reciter/save/userfeedback/[uid].ts
    src/pages/api/reciter/update/goldstandard.ts
    src/components/elements/AddUser/AddUser.tsx
    src/components/elements/AddUser/CurationScopeSection.tsx
    controllers/db/manage-users/user.controller.ts
    src/components/elements/Manage/ManageUsers.tsx
    src/components/elements/Manage/UsersTable.tsx
    src/components/elements/Search/Search.js
    src/components/elements/Search/ScopeFilterCheckbox.tsx
    src/components/elements/Navbar/SideNavbar.tsx
    src/components/elements/Navbar/ScopeLabel.tsx
    src/pages/curate/[id].js
    src/pages/manageprofile/[userId].tsx
    src/components/elements/ManageProfile/ManageProfile.tsx

    <!-- 2 EXISTING TEST FILES -->
    __tests__/utils/scopeResolver.test.ts
    __tests__/utils/constants-scoped.test.ts
  </read_first>
  <action>
    This is a READ-ONLY verification task. Do NOT modify any source files. Only create the VERIFICATION.md document.

    **Step 1: Run existing tests and capture results**

    Run both test suites and record pass/fail output:
    ```
    npx jest --testPathPattern="scopeResolver" --no-coverage --verbose 2>&1
    npx jest --testPathPattern="constants-scoped" --no-coverage --verbose 2>&1
    ```
    Record: total tests, pass count, fail count, and any failure details.

    **Step 2: Read all 16+ key source files**

    Read every file listed in read_first above. For each file, note:
    - File exists and is non-empty
    - Key exports, functions, or patterns present
    - Line numbers for critical code sections

    **Step 3: Verify all 36 Observable Truths**

    For each truth from the Phase 3 plan must_haves.truths blocks, verify by reading actual source code. The 36 truths are:

    Plan 01 truths (6):
    1. getCapabilities() recognizes Curator_Scoped role and returns canCurate.scoped = true
       -> Verify in src/utils/constants.js: ROLE_CAPABILITIES has Curator_Scoped entry, getCapabilities merge logic sets scoped flag
    2. isPersonInScope() correctly evaluates AND across dimensions and OR within dimensions
       -> Verify in src/utils/scopeResolver.ts: confirm AND/OR logic in function body
    3. Null personTypes in scope means no restriction on person type axis
       -> Verify in src/utils/scopeResolver.ts: null check skips personType filtering
    4. Null orgUnits in scope means no restriction on org unit axis
       -> Verify in src/utils/scopeResolver.ts: null check skips orgUnit filtering
    5. AdminUsersPersonType Sequelize model exists and maps to admin_users_person_types table
       -> Verify in src/db/models/AdminUsersPersonType.ts: tableName = 'admin_users_person_types'
    6. Curator_Scoped is registered in ROLE_CAPABILITIES with canSearch: true and canCurate: { scoped: true }
       -> Verify in src/utils/constants.js: exact entry in ROLE_CAPABILITIES

    Plan 02 truths (7):
    7. findUserPermissions returns scopeData (personTypes + orgUnits) for Curator_Scoped users
       -> Verify in controllers/db/userroles.controller.ts: scopeData in return object
    8. JWT token contains scopeData alongside userRoles for Curator_Scoped users
       -> Verify in src/pages/api/auth/[...nextauth].jsx: token.scopeData assignment in JWT callback
    9. Middleware allows Curator_Scoped users to access /curate/* routes
       -> Verify in src/middleware.ts: Curator_Scoped handling for curate routes
    10. Middleware adds /manageprofile/* to route matcher
        -> Verify in src/middleware.ts: config.matcher includes manageprofile pattern
    11. userfeedback save API returns 403 when scoped curator tries to save for out-of-scope person
        -> Verify in src/pages/api/reciter/save/userfeedback/[uid].ts: isPersonInScope check + 403 response
    12. goldstandard update API returns 403 when scoped curator tries to update for out-of-scope person
        -> Verify in src/pages/api/reciter/update/goldstandard.ts: isPersonInScope check + 403 response
    13. Person search controller supports scope filtering via scopePersonTypes and scopeOrgUnits parameters
        -> Verify in controllers/db/person.controller.ts: scopePersonTypes/scopeOrgUnits in WHERE clause

    Plan 03 truths (8):
    14. When Curator_Scoped is selected in AddUser form, a Curation Scope section appears
        -> Verify in src/components/elements/AddUser/AddUser.tsx: conditional render of CurationScopeSection
    15. When Curator_All and Curator_Scoped are both selected, form shows mutual exclusion error
        -> Verify in src/components/elements/AddUser/AddUser.tsx: mutual exclusion validation logic
    16. If Curator_Scoped is selected with no scope fields, form shows 'At least one' error
        -> Verify in src/components/elements/AddUser/AddUser.tsx or CurationScopeSection.tsx: validation for empty scope
    17. Saving a Curator_Scoped user persists person types to admin_users_person_types
        -> Verify in controllers/db/manage-users/user.controller.ts: bulkCreate or create for AdminUsersPersonType
    18. Editing an existing Curator_Scoped user pre-populates scope fields
        -> Verify in src/components/elements/AddUser/AddUser.tsx: fetch/load of existing person types on edit
    19. UsersTable shows roles column with scope summary
        -> Verify in src/components/elements/Manage/UsersTable.tsx: roles column rendering with scope labels
    20. ManageUsers page has a role filter dropdown
        -> Verify in src/components/elements/Manage/ManageUsers.tsx: role filter select/dropdown element
    21. Server-side validation rejects simultaneous Curator_All + Curator_Scoped
        -> Verify in controllers/db/manage-users/user.controller.ts: mutual exclusion check + error response

    Plan 04 truths (10):
    22. Scoped curator sees 'Show only people I can curate' checkbox
        -> Verify in src/components/elements/Search/ScopeFilterCheckbox.tsx: checkbox with that label text
    23. Checkbox is NOT visible for Curator_All, Curator_Self-only, or Reporter_All-only users
        -> Verify in src/components/elements/Search/Search.js: conditional render based on canCurate.scoped
    24. Checking the box filters results to only people matching scope
        -> Verify in src/components/elements/Search/Search.js: scope filter params sent to API when checked
    25. In-scope people show curate icon (EditOutlined)
        -> Verify in src/components/elements/Search/Search.js: EditOutlined icon conditionally rendered
    26. Out-of-scope people do NOT show curate icon
        -> Verify in src/components/elements/Search/Search.js: icon render condition excludes out-of-scope
    27. Clicking in-scope person name navigates to /curate/:id
        -> Verify in src/components/elements/Search/Search.js: link href to /curate/ for in-scope
    28. Clicking out-of-scope person name navigates to /report
        -> Verify in src/components/elements/Search/Search.js: link href to /report for out-of-scope
    29. Scope label appears in sidebar: 'Curating: ...'
        -> Verify in src/components/elements/Navbar/ScopeLabel.tsx: "Curating:" text in output
    30. Curate page checks scope on mount and redirects with toast
        -> Verify in src/pages/curate/[id].js: useEffect with isPersonInScope check + redirect + toast
    31. Checkbox state persists across pagination
        -> Verify in src/components/elements/Search/Search.js: state management (Redux or local) for checkbox survives re-render

    Plan 05 truths (5):
    32. Manage Profile page renders ORCID management for a person
        -> Verify in src/components/elements/ManageProfile/ManageProfile.tsx: ORCID-related content
    33. Scoped curator can access Manage Profile for in-scope people
        -> Verify in src/pages/manageprofile/[userId].tsx: scope check passes for in-scope
    34. Scoped curator is redirected with toast for out-of-scope person
        -> Verify in src/pages/manageprofile/[userId].tsx: isPersonInScope check + redirect + toast
    35. Curator_All can access Manage Profile for any person
        -> Verify in src/pages/manageprofile/[userId].tsx: Curator_All bypass of scope check
    36. 5 files ported from master commit 544c0f2
        -> Verify existence of 5 files: controllers/db/manage-profile/manageProfile.controller.ts, src/components/elements/ManageProfile/ManageProfile.tsx, src/components/elements/ManageProfile/ManageProfile.module.css, src/pages/api/db/admin/manageProfile/getORCIDProfileDataByID/index.ts, src/pages/manageprofile/[userId].tsx

    For EACH truth, record:
    - Status: VERIFIED (confirmed in code), WARN (partially confirmed), FAILED (not found or wrong), HUMAN (needs manual testing)
    - Evidence: specific file path, line number or content snippet, and why it confirms/refutes the truth

    **Step 4: Verify all 14 Key Links**

    For each key link from plan must_haves.key_links, trace the import/export/call chain:

    Plan 01 links (2):
    1. constants.js -> ROLE_CAPABILITIES (Curator_Scoped entry)
    2. init-models.ts -> AdminUsersPersonType.ts (import + initModel + association)

    Plan 02 links (3):
    3. userroles.controller.ts -> [...nextauth].jsx (findUserPermissions return -> JWT callback)
    4. [...nextauth].jsx -> middleware.ts (JWT token scopeData -> middleware decode)
    5. userfeedback/[uid].ts -> scopeResolver.ts (isPersonInScope import)

    Plan 03 links (3):
    6. AddUser.tsx -> CurationScopeSection.tsx (conditional render)
    7. AddUser.tsx -> user.controller.ts (form submit -> createOrUpdateAdminUser)
    8. ManageUsers.tsx -> UsersTable.tsx (passes user data with roles)

    Plan 04 links (4):
    9. Search.js -> ScopeFilterCheckbox.tsx (conditional render based on canCurate.scoped)
    10. Search.js -> person.controller.ts (scope filter params to /api/db/person)
    11. SideNavbar.tsx -> ScopeLabel.tsx (conditional render when canCurate.scoped)
    12. curate/[id].js -> scopeResolver.ts (scope check on mount)

    Plan 05 links (2):
    13. manageprofile/[userId].tsx -> ManageProfile.tsx (page imports component)
    14. manageprofile/[userId].tsx -> scopeResolver.ts (scope check on mount)

    For EACH link, record:
    - Status: WIRED (import/call confirmed) or BROKEN (missing or wrong)
    - Details: specific import statement, line number, call site

    **Step 5: Scan for anti-patterns**

    Search ALL Phase 3 source files for:
    - TODO, FIXME, HACK, XXX comments
    - Placeholder/stub implementations (return null, empty handlers, hardcoded values that should be dynamic)
    - console.log statements that shouldn't be in production code (intentional [AUTH] or [SCOPE] logging is fine)
    - Dead code or unreachable branches

    Use grep across all Phase 3 files:
    ```
    grep -rn "TODO\|FIXME\|HACK\|XXX\|placeholder\|stub" src/utils/scopeResolver.ts src/utils/constants.js src/db/models/AdminUsersPersonType.ts ...
    ```

    **Step 6: Produce the VERIFICATION.md document**

    Write to `.planning/phases/03-scoped-curation-roles/03-VERIFICATION.md` following the Phase 4 VERIFICATION.md format EXACTLY. Include these sections:

    Frontmatter:
    ```yaml
    ---
    phase: 03-scoped-curation-roles
    verified: {current ISO timestamp}
    status: {passed|failed|partial}
    score: {N}/36 must-haves verified
    re_verification: false
    ---
    ```

    Sections (in order):
    1. **Header** -- Phase Goal, Verified timestamp, Status, Re-verification flag
    2. **Goal Achievement > Observable Truths** -- Table with all 36 truths, status, evidence (cite file:line, not SUMMARY)
    3. **Goal Achievement > Required Artifacts** -- Table confirming all Phase 3 created/modified files exist with expected content
    4. **Key Link Verification** -- Table with all 14 links, status, details
    5. **Requirements Coverage** -- Table mapping each SCOPE-01 through SCOPE-06 to evidence from truths/links, with SATISFIED/PARTIAL/FAILED status
    6. **Anti-Patterns Found** -- Table with file, line, pattern, severity, impact
    7. **Human Verification Required** -- Table with behaviors that need manual testing (scope label rendering, form show/hide, toast messages, redirect behavior per CONTEXT.md)
    8. **Test Coverage Assessment** -- Table mapping each SCOPE requirement to existing test files, coverage level, and gaps vs VALIDATION.md Wave 0 stubs
    9. **Gaps Summary** -- Narrative summary of all issues found, with severity classification

    CRITICAL rules for evidence:
    - NEVER cite SUMMARY files as evidence -- always cite the actual source file path and line/content
    - For each truth, include enough detail that a reader can confirm the finding without re-reading the source
    - For HUMAN items in Observable Truths, mark status as HUMAN and explain why in evidence column
    - Note in Requirements Coverage where Phase 4 also modified the same files (userfeedback, goldstandard, curate, manageprofile, Search.js, SideNavbar.tsx, person.controller.ts) and confirm Phase 3 code is still intact after Phase 4 modifications

    Test Coverage Assessment must reference:
    - __tests__/utils/scopeResolver.test.ts (13 tests) -- covers SCOPE-06 (flexible combinations)
    - __tests__/utils/constants-scoped.test.ts (14 tests) -- covers SCOPE-01 (capability model)
    - Note that VALIDATION.md planned 7 test stubs in __tests__/phase03/ that were never created (0/7)
    - Note missing coverage: no component tests (SCOPE-02, SCOPE-04, SCOPE-05), no API endpoint tests (SCOPE-03), no integration tests
  </action>
  <verify>
    <automated>test -f .planning/phases/03-scoped-curation-roles/03-VERIFICATION.md && grep -c "SCOPE-0[1-6]" .planning/phases/03-scoped-curation-roles/03-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/03-scoped-curation-roles/03-VERIFICATION.md exists and is non-empty
    - File contains "Observable Truths" section header
    - File contains "Key Link Verification" section header
    - File contains "Requirements Coverage" section header
    - File contains "Anti-Patterns Found" section header
    - File contains "Human Verification Required" section header
    - File contains "Test Coverage Assessment" section header
    - File contains "Gaps Summary" section header
    - All 6 requirement IDs appear: grep -c "SCOPE-01" returns >= 2, same for SCOPE-02 through SCOPE-06
    - Truth table has 36 rows (grep -c "| VERIFIED\|| WARN\|| FAILED\|| HUMAN" returns >= 36)
    - Key link table has 14 rows (grep -c "| WIRED\|| BROKEN" returns >= 14)
    - No evidence column references "per SUMMARY" or "claimed in 03-XX-SUMMARY.md" (verification must be independent)
    - Frontmatter contains "score:" field
    - Frontmatter status is "passed", "failed", or "partial"
  </acceptance_criteria>
  <done>
    Complete VERIFICATION.md exists at .planning/phases/03-scoped-curation-roles/03-VERIFICATION.md with all 36 truths verified against actual source code (not SUMMARY claims), all 14 key links traced, all 6 SCOPE requirements mapped to evidence, anti-patterns documented, human verification items listed, and test coverage assessed. The document follows the Phase 4 VERIFICATION.md format exactly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md and STATE.md based on verification results</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    .planning/phases/03-scoped-curation-roles/03-VERIFICATION.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
  </read_first>
  <action>
    Read the VERIFICATION.md produced by Task 1 and update project tracking documents.

    **Step 1: Update REQUIREMENTS.md traceability**

    For each SCOPE requirement (SCOPE-01 through SCOPE-06):
    - If VERIFICATION.md status is SATISFIED: change checkbox from `- [ ]` to `- [x]` and update traceability table status from "Pending" to "Complete -- {brief evidence summary}"
    - If VERIFICATION.md status is PARTIAL: keep `- [ ]` but update traceability table status to "Partial -- {what's missing}"
    - If VERIFICATION.md status is FAILED: keep `- [ ]` and update traceability table status to "Failed -- {what's wrong}"

    Also update the Coverage section counts:
    - Recalculate "Complete" count based on new checkbox states
    - Recalculate "Pending (gap closure)" count

    **Step 2: Update STATE.md**

    Update the following fields:
    - `stopped_at`: "Phase 5 verification complete"
    - `last_updated`: current ISO timestamp
    - `last_activity`: current date + "Completed Plan 05-01 (Phase 3 independent verification) -- {N}/36 truths verified, {status}"
    - `progress.completed_phases`: increment if appropriate
    - Under `Current Position`: update Phase to "5 of 6" and Plan status

    Add to Accumulated Context > Decisions:
    - [Phase 05]: Phase 3 SCOPE requirements verified with {N}/36 truths, {M}/14 links confirmed
    - Any specific findings that affect future work (e.g., if any requirement is PARTIAL or FAILED, note the gap)

    **Step 3: Update v1.0-MILESTONE-AUDIT.md if it exists**

    If `.planning/v1.0-MILESTONE-AUDIT.md` exists, add a note that Phase 5 verification is complete and reference the VERIFICATION.md location.
  </action>
  <verify>
    <automated>grep -cE "SCOPE-0[1-6].*(Complete|Partial|Failed)" .planning/REQUIREMENTS.md && grep "Phase 5" .planning/STATE.md</automated>
  </verify>
  <done>
    REQUIREMENTS.md reflects verification results for all 6 SCOPE requirements (checkboxes and traceability table updated with Complete/Partial/Failed status -- no rows still say "Pending"), STATE.md records Phase 5 completion with verification summary, and milestone audit (if present) references the verification output.
  </done>
</task>

</tasks>

<verification>
Phase 5 verification is complete when:
1. `.planning/phases/03-scoped-curation-roles/03-VERIFICATION.md` exists with all required sections
2. All 36 observable truths have a status (VERIFIED/WARN/FAILED/HUMAN) with source-code evidence
3. All 14 key links have a status (WIRED/BROKEN) with import/call chain evidence
4. All 6 SCOPE requirements have a status (SATISFIED/PARTIAL/FAILED) with mapped evidence
5. No evidence references SUMMARY files (independent verification)
6. Existing tests (27 total) pass: `npx jest --testPathPattern="(scopeResolver|constants-scoped)" --no-coverage`
7. REQUIREMENTS.md traceability updated based on findings
8. STATE.md updated with Phase 5 completion
</verification>

<success_criteria>
- 03-VERIFICATION.md is a complete, structured verification report that would allow any reviewer to understand the verification status of Phase 3 scoped curation roles without reading any other document
- Every SCOPE requirement has clear SATISFIED/PARTIAL/FAILED status with specific evidence
- The document follows the Phase 4 VERIFICATION.md format (Observable Truths table, Key Links table, Requirements Coverage table, Anti-Patterns, Human Verification Required, Gaps Summary)
- Test coverage gaps are documented (existing 27 tests vs planned 7 Wave 0 stubs that were never created)
- REQUIREMENTS.md and STATE.md reflect the verification results
</success_criteria>

<output>
After completion, create `.planning/phases/05-phase3-verification/05-01-SUMMARY.md`
</output>
