---
context: default
phase: null
status: paused
last_updated: 2026-04-06T16:13:42.267Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: package-lock.json — `npm ci` fails in CodeBuild with `Missing: string_decoder@1.1.1`. Local `npm install` does not fix it. Must be regenerated inside a `node:20-alpine` container.

**Do not trigger another pipeline run until this is fixed.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Local npm mismatch | Running `npm install` locally (npm 10.9.4) does not reproduce the build container's npm 10.8.2 lock file requirements | blocking | Always regenerate package-lock.json inside `node:20-alpine` container when fixing lock file issues |

<current_state>
Bug fix code is committed and pushed (064a91f, f1cb7c3). CodeBuild pipeline for `ReCiter-Publication-Manager-Dev` is failing at the Build stage — not because of the code changes, but because of a pre-existing package-lock.json issue. BUILD-ISSUE.md documents the full details. The fix (immutable parent updates + child useEffect re-sync) has NOT yet been tested on reciter-dev because the build can't complete.
</current_state>

<completed_work>

- **064a91f** `fix(curate): immutable parent updates + child re-sync so cards actually disappear on assert`
  - `ReciterTabs.tsx`: replaced `splice`/`push` mutation with `setFilteredData(prev => prev.map(...))` returning new object/array references
  - `ReciterTabContent.tsx`: added `useEffect([props.publications])` that re-syncs local state from parent, re-applying active text filter

- **f1cb7c3** `fix(deps): update package-lock.json` — partial fix, insufficient (same error persists)

- **BUILD-ISSUE.md** created (uncommitted) — full diagnosis + fix instructions
</completed_work>

<remaining_work>

1. Commit `BUILD-ISSUE.md`
2. Fix `package-lock.json` using docker:
   ```bash
   docker run --rm -v $(pwd):/app -w /app node:20-alpine \
     sh -c "npm install --legacy-peer-deps"
   git add package-lock.json
   git commit -m "fix(deps): regenerate package-lock.json inside node:20-alpine"
   git push origin dev_Upd_NextJS14SNode18
   ```
3. Trigger + approve pipeline (`ReCiter-Publication-Manager-Dev`)
4. Verify reject/accept on Suggested tab removes cards immediately on reciter-dev
</remaining_work>

<decisions_made>

- Root cause: `updatePublicationAssertion` in ReciterTabs mutated `tabData.data` in-place (splice/push on shallow-copied array). This meant `props.publications` in the child never received a new array reference, so `useEffect([props.publications])` could never detect parent updates.
- Fix: two-part — (1) immutable parent updates, (2) child `useEffect` sync. The ba162b8 `setPublications(filter)` stays as an optimization for immediate feedback.
</decisions_made>

<blockers>
- **BUILD-ISSUE.md** is uncommitted (minor)
- **package-lock.json** still broken for npm 10.8.2 inside `node:20-alpine` — see `BUILD-ISSUE.md` for docker-based fix command
</blockers>

## Required Reading

1. `BUILD-ISSUE.md` — full diagnosis of the npm ci failure, exact docker command to fix
2. `src/components/elements/CurateIndividual/ReciterTabs.tsx` — see `updatePublicationAssertion` (line ~62)
3. `src/components/elements/CurateIndividual/ReciterTabContent.tsx` — see new `useEffect` (line ~58)

## Infrastructure State

- Branch: `dev_Upd_NextJS14SNode18`
- Pipeline: `ReCiter-Publication-Manager-Dev` (AWS CodePipeline, us-east-1) — last execution failed at Build stage
- reciter-dev: still running the pre-fix image (build never completed)

<context>
The actual React bug fix is solid and done. Only thing blocking deployment is the lock file issue that pre-dates today's work (last good build was 2026-02-09). Once docker-based npm install fixes the lock file and the build succeeds, the fix should be testable on reciter-dev immediately.
</context>

<next_action>
Start with: `git add BUILD-ISSUE.md && git commit -m "docs: add BUILD-ISSUE.md documenting npm ci lock file failure"`, then run the docker command above to fix package-lock.json.
</next_action>
